/**
 * Delt density-type for legal-seksjoner.
 *
 * `full` = desktop (lang prosa, alle sub-elementer som GDPR-grid, step-row osv.)
 * `compact` = mobil (kortere copy, enklere layouts for smalere viewport)
 */
export type Density = 'full' | 'compact';
